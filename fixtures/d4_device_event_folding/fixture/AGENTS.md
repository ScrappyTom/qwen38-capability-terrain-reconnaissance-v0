# Fixture instructions

The governing behavior is documented in `docs/device-events.md`. Read it before changing the implementation.

Only `src/fold-device-events.mjs` is an intended implementation target. Do not modify tests, documentation, package metadata, or this instruction file. Use `npm test` for the visible suite.
