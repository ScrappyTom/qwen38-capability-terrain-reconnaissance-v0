Fix `planDeploymentWaves` according to `docs/deployment-waves.md`. The intended implementation target is `src/deployment-waves.mjs`, and the visible tests are in `test/deployment-waves.test.mjs`. Preserve the public API and the behavior of the other exported helpers. Do not weaken or edit tests, documentation, package metadata, or repository instructions. Run `npm test` before you finish.

Your final response must contain these exact labels in this order, with substantive content after each label:

Root cause:
Files changed:
Verification:
Remaining limitations:

