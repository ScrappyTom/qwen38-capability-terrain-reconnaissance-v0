# Fixture instructions

The governing behavior for `planDeploymentWaves` is documented in `docs/deployment-waves.md`.

Only `src/deployment-waves.mjs` is an intended implementation target. Preserve the other exported helpers. Do not modify tests, documentation, package metadata, or this instruction file. Use `npm test` for the visible suite.

